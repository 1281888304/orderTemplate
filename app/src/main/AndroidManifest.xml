<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ordertemplate">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OrderTemplate">
        <activity android:name=".ui.slideshow.ContractInformation"></activity>
        <activity android:name=".ui.powder.powderClickViewActivity" />
        <activity android:name=".ui.topping.ToppingViewActivity" />
        <activity
            android:name=".Orders"
            android:label="@string/title_activity_orders"
            android:theme="@style/Theme.OrderTemplate.NoActionBar"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".MainActivity">

            <!-- android:windowSoftInputMode="stateHidden" -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>